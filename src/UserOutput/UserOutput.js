import React from 'react';

const useroutput = (props) => {
        return (
            <div>
                <p> i am useroutput cmpn name is {props.name} and color {props.color} </p>
            </div>
        )
    };
    
export default useroutput;